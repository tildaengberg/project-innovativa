import { View } from 'react-native'
import MarginBox from '../config/MarginBox'
import StyledText from '../config/StyledText'

const Memories = () => {
  return (
    <View style={MarginBox.container}>
      <StyledText>Minnen</StyledText>
    </View>
  )
}

export default Memories
